#include"main.h"


void opcontrol()
{



	//if (!reader->parse(&*(stu.begin()), &*(stu.end()), &root, &err)) {
	//	throw std::invalid_argument("Error during JSON-string interpretation.");
	//}
	//std::vector<std::string> v = root["Skills"].asString();

		//v.push_back(root["数组"].asInt());
	//for (auto &it : v)
	//	std::cout << it << std::endl;
	//Json::Reader reader;
	//std::fstream fs("");
	//if (reader.parse(stu, root))  // reader将Json字符串解析到root，root将包含Json里所有子元素  
	//{
	//	std::cout <<root<< std::endl;

	//

	//}

	//Json::Value root;
	//Json::Reader reader;
	//std::ifstream ifs("example.json");//open file example.json

	//if (!reader.parse(ifs, root)) {
	//	// fail to parse
	//}
	//else {
	//	// success
	//	std::cout << root["你好"].asString() << std::endl;
	//	std::cout << root["indent"]["length"].asInt() << std::endl;
	//}

	//ncrapi::Poten poten(1, 5);
	//std::fstream fs("Text1.txt");
	//if (!fs.is_open())
	//	std::cerr << "file open error" << std::endl;
	//std::string temp;
	//while (std::getline(fs, temp))
	//{
	//	std::istringstream iss(temp);
	//	int val;
	//	iss >> val;
	//	poten.get(val, 1);
	//	poten.print();
	//	Sleep(10);
	//}
	//fs.close();

	//size_t i = 0;

	//while (i<=100)
	//{
	//	chassis->forward(127);
	//	lift->set(50);
	//	intake->set(-10);
	//	shoot->set(20);
	//	for (auto &it : sysData->_objData)
	//		it->addRecoderData();
	//	i++;
	//	Sleep(100);
	//}
	//sysData->showRecoderData();

}